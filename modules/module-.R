library(shiny)
library(shinyjs)
library(bslib)
library(bsicons)

# ui
module_xxx <- function(id) {
  ns <- NS(id)
  # UI Module
  fluidPage(
    
  )
}

# server
server_xxx <- function(id) {
  moduleServer(id, function(input, output, server) {
    # Server Module
    
  })
}
